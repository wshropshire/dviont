#conda env export --from-history > dviont_env.yaml

name: dviont_env
channels:
  - bioconda
  - conda-forge
  - r
dependencies:
  - python=3.10
  - clair3
  - ca-certificates
  - openssl
  - minimap2
  - certifi
  - snpeff
  - bcftools
prefix: <path/to/virtual/env/>
